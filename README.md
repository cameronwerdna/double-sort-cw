solution for codewars problem 'Double Sort'

function takes an array of numbers and strings and returns a single array, with the numbers sorted in ascending order and the strings sorted alphabetically.